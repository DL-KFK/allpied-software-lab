# Вказуємо базовий образ — легкий веб-сервер
FROM nginx:alpine

# Встановлюємо робочий каталог
WORKDIR /usr/share/nginx/html

# Копіюємо файли репозиторію в каталог веб-контенту
COPY . .

# Вказуємо, що порту 80 буде використовуватись
EXPOSE 80

# Встановлюємо команду запуску
CMD ["nginx", "-g", "daemon off;"]
